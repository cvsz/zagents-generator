# Codex skill manifest for create-gemini.
#
# Codex doesn't ship a first-class "marketplace" yet but it does honor
# skill packages installed under ~/.codex/skills/ or .codex/skills/. This
# manifest declares the skill's surface and the MCP server it dispatches to.

[skill]
name = "create-gemini"
version = "0.1.0"
description = "Scaffold your own focused AI agent gemini — pick host (Claude Code, Codex, pi.dev, Hermes), template, agents, skills, and ship a npm-publishable gemini with its own npx CLI."
author = "rUv <ruv@ruv.net>"
homepage = "https://github.com/cvsz/zagents-generator"
license = "MIT"

# Mapped onto a single MCP tool invocation. Codex calls this tool with
# the user's responses to the prompts below.
[dispatch]
type = "mcp_tool"
server = "zagents-generator"
tool = "create_harness"

# Codex's slash-command surface — what `/create-gemini` expands to.
[command]
name = "create-gemini"
synopsis = "Scaffold a new AI agent gemini"

# Argument shape Codex passes to the MCP tool.
[[args]]
name = "name"
prompt = "Gemini name (kebab-case)"
required = true
validate = "^[a-z0-9-]+$"

[[args]]
name = "description"
prompt = "One-line description"
default = "My AI agent gemini"

[[args]]
name = "host"
prompt = "Host adapter"
default = "claude-code"
choices = ["claude-code", "codex", "pi-dev", "hermes", "openclaw", "rvm"]

[[args]]
name = "template"
prompt = "Template"
default = "minimal"
choices = ["minimal", "vertical:trading", "vertical:devops", "vertical:legal", "vertical:support", "vertical:research", "eject-from-ruflo"]

[[args]]
name = "branding"
prompt = "Branding mode"
default = "independent"
choices = ["independent", "powered-by"]

# Tags used by codex's skill catalog (analog to Claude Code's plugin tags).
[catalog]
tags = ["scaffolding", "agent-gemini", "mcp", "multi-agent", "rust", "wasm"]
