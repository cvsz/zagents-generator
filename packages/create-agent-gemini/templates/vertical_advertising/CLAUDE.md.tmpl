# {{name}}

{{description}}

> Advertising gemini · domain: `advertising/media`. Generated with [create-agent-gemini](https://github.com/ruvnet/agent-gemini-generator).

## Behavioral rules

- Use the gemini's MCP tools (`mcp__{{name}}__*`) for orchestration
- Memory and routing are handled by the kernel — you don't need to learn them
- Defer destructive operations to the user

## Agents

| Agent | Tier | Role |
|---|---|---|
| `media-planner` | opus | Allocates budget across channels. |
| `copywriter` | sonnet | Writes copy to the channel and format. |
| `performance-analyst` | sonnet | Reads results and reallocates spend. |
## Skills

- `/media-plan` — Build a cross-channel media plan with budget split, creative, and KPIs.

## Commands

- `doctor` — Health-check the gemini: kernel load, MCP wiring, memory backend, host adapter.

## Architecture

This gemini uses [@metaharness/kernel](https://www.npmjs.com/package/@metaharness/kernel) — a Rust-compiled WASM module with a NAPI-RS native fallback — so the same code runs identically on every platform.
