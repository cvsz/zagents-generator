# {{name}}

{{description}}

> **Business Operations** — Analyst → strategist → ops-coordinator, with a metrics MCP for KPI grounding.
>
> Generated with [`create-agent-gemini`](https://github.com/ruvnet/agent-gemini-generator). Multi-host scaffolding with a kernel that resolves native → wasm → js (js backend in the published beta; see `gemini doctor`).

## Install

```bash
npm install -g {{name}}
{{name}} init
{{name}} doctor
```

## Agents

| Agent | Role |
|---|---|
| `analyst` | Turns raw metrics into findings. |
| `strategist` | Chooses the bet and the trade-offs. |
| `ops-coordinator` | Turns the chosen bet into owned actions. |

This gemini ships with the **{{host}}** adapter.

## License

MIT
