# {{name}}

{{description}}

> Business Operations gemini · domain: `business/strategy`. Generated with [create-agent-gemini](https://github.com/ruvnet/zagents-generator).

## Behavioral rules

- Use the gemini's MCP tools (`mcp__{{name}}__*`) for orchestration
- Memory and routing are handled by the kernel — you don't need to learn them
- Defer destructive operations to the user

## Agents

| Agent | Tier | Role |
|---|---|---|
| `analyst` | sonnet | Turns raw metrics into findings. |
| `strategist` | opus | Chooses the bet and the trade-offs. |
| `ops-coordinator` | sonnet | Turns the chosen bet into owned actions. |
## Skills

- `/quarterly-plan` — Build a quarterly plan: findings → strategy → owned action items tied to KPIs.

## Commands

- `doctor` — Health-check the gemini: kernel load, MCP wiring, memory backend, host adapter.

## Architecture

This gemini uses [@zagents/kernel](https://www.npmjs.com/package/@zagents/kernel) — a Rust-compiled WASM module with a NAPI-RS native fallback — so the same code runs identically on every platform.
